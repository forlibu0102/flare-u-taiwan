<footer>
  <div class="footer-inner">
    <div class="footer-brand">
      <strong>FLARE U Taiwan</strong>
      <p>Created by Taiwan U'RE, for every U'RE.</p>
	  <a href="/site-about">關於本站與內容聲明</a>
    </div>

    <div class="contact-links" aria-label="與我聯繫">
      <span>與我聯繫</span>
      <a
        class="social-link"
        href="https://www.instagram.com/for_libu0102/"
        target="_blank"
        rel="noreferrer"
        aria-label="前往 Instagram 聯繫 for_libu0102"
      >
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <rect x="3" y="3" width="18" height="18" rx="5"></rect>
          <circle cx="12" cy="12" r="4"></circle>
          <circle class="social-icon-dot" cx="17.4" cy="6.6" r="1"></circle>
        </svg>
        <span>Instagram</span>
        <b aria-hidden="true">↗</b>
      </a>
    </div>

    <p class="copyright">
      © 2026 FLARE U Taiwan
    </p>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid #e5e7eb;
    padding: 32px 24px;
    background: #ffffff;
  }

  .footer-inner {
    width: min(1200px, calc(100% - 48px));
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto auto;
    align-items: center;
    gap: 24px;
  }

  strong {
    font-size: 1rem;
  }

  p {
    margin: 8px 0 0;
    color: #667085;
    font-size: 0.9rem;
  }

  a {
    display: inline-block;
    margin-top: 12px;
    color: #526176;
    font-size: 0.84rem;
    text-underline-offset: 3px;
  }

  .copyright {
    white-space: nowrap;
  }

  .contact-links {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .contact-links > span {
    color: #667085;
    font-size: 0.84rem;
    font-weight: 700;
    white-space: nowrap;
  }

  .social-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin: 0;
    padding: 9px 13px;
    border: 1px solid #dbe3ee;
    border-radius: 999px;
    background: #f8fafc;
    color: #263850;
    font-weight: 700;
    text-decoration: none;
    transition: border-color .2s ease, color .2s ease, transform .2s ease;
  }

  .social-link:hover {
    border-color: #c84997;
    color: #b53683;
    transform: translateY(-2px);
  }

  .social-link:focus-visible {
    outline: 3px solid rgba(200, 73, 151, .25);
    outline-offset: 3px;
  }

  .social-link svg {
    width: 18px;
    height: 18px;
    fill: none;
    stroke: currentColor;
    stroke-width: 1.8;
  }

  .social-link .social-icon-dot {
    fill: currentColor;
    stroke: none;
  }

  .social-link b {
    font-size: .9rem;
  }

  @media (max-width: 700px) {
    .footer-inner {
      width: min(100%, calc(100% - 16px));
      grid-template-columns: 1fr;
      align-items: flex-start;
    }

    .contact-links {
      flex-wrap: wrap;
    }
  }

  :global(html[data-theme="dark"]) footer {
    border-color: #25324a;
    background: #081225;
  }

  :global(html[data-theme="dark"]) footer strong { color: #f2f6fb; }
  :global(html[data-theme="dark"]) footer p,
  :global(html[data-theme="dark"]) footer a { color: #9eacc0; }
  :global(html[data-theme="dark"]) .contact-links > span { color: #9eacc0; }
  :global(html[data-theme="dark"]) .social-link {
    border-color: #34445d;
    background: #101f38;
    color: #d9e4f2;
  }
  :global(html[data-theme="dark"]) .social-link:hover {
    border-color: #d75aa7;
    color: #f08ac7;
  }
</style>
